---
phase: 04-database-performance
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - actions/reviews.ts
  - app/(storefront)/account/page.tsx
autonomous: true
requirements: [PERF-05, PERF-07]
must_haves:
  truths:
    - "getProductReviews() accepts page/limit options and returns { data: ProductReview[], total: number, page: number, pageSize: number } using .range() and { count: 'exact' }"
    - "Account page orders query uses .range() for pagination with page from searchParams"
    - "Account page renders pagination controls when total orders > pageSize"
    - "Storefront page size is 20 items for reviews and account order history"
  artifacts: []
  key_links:
    - "actions/reviews.ts getProductReviews() -> .select('*', { count: 'exact' }).range(from, to)"
    - "app/(storefront)/account/page.tsx -> reads searchParams.page -> uses .range() on orders query"
---

<objective>
Add server-side pagination to product reviews and account order history.

Purpose: Product reviews and account order history currently load ALL rows with no pagination. Both endpoints need `.range()` pagination with `{ count: 'exact' }`.

Output: Updated getProductReviews with pagination, updated account page with paginated orders and pagination controls.
</objective>

<execution_context>
@/Users/richard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@actions/reviews.ts
@app/(storefront)/account/page.tsx
@lib/pagination.ts
@components/storefront/pagination-controls.tsx

<interfaces>
<!-- Existing pagination utility from lib/pagination.ts -->
<!-- Pattern: parsePagination({ page, limit }) -> { page, pageSize, from, to } -->
<!-- Supabase: .select("*", { count: "exact" }).range(from, to) -->
<!-- PaginationControls component created in plan 03 (wave 1, no hard dependency -- if 04 executes first, create a minimal version or just add the action logic) -->

From lib/pagination.ts:
```typescript
export function parsePagination(options?: {
  page?: number;
  limit?: number;
}): { page: number; pageSize: number; from: number; to: number }
```

Current getProductReviews return type (to be changed):
```typescript
export async function getProductReviews(productId: string): Promise<{
  data: ProductReview[]; error: string | null;
}>
```

Current account page orders query (to be changed):
```typescript
const { data: orders } = await supabase
  .from("orders")
  .select("*")
  .eq("user_id", user.id)
  .order("created_at", { ascending: false });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pagination to getProductReviews</name>
  <files>actions/reviews.ts</files>
  <action>
**Update `actions/reviews.ts` -- `getProductReviews()`:**
- Add optional `options?: { page?: number; limit?: number }` parameter.
- Update return type to: `Promise<{ data: ProductReview[], total: number, page: number, pageSize: number, error: string | null }>`.
- Add `{ count: "exact" }` to `.select()`.
- Use `parsePagination({ page: options?.page, limit: options?.limit ?? 20 })` to get `from, to`.
- Add `.range(from, to)` to the query.
- Return `{ data, total: count ?? 0, page, pageSize, error: null }`.
- Import `parsePagination` from `@/lib/pagination`.
- NOTE: Product reviews are NOT currently displayed on the product detail page. The pagination is added to the action function per PERF-05. Wiring to the product page UI is out of scope -- reviews display will be added when the product page is updated to show reviews.
- Update the Zod schema for the options to validate page/limit.
  </action>
  <verify>
    <automated>grep -q "count.*exact" actions/reviews.ts && grep -q "range" actions/reviews.ts && echo "PASS: Reviews pagination added" || echo "FAIL"</automated>
  </verify>
  <done>getProductReviews() uses .range() with { count: 'exact' }. Return type includes pagination metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Add pagination to account order history</name>
  <files>app/(storefront)/account/page.tsx</files>
  <action>
**Update `app/(storefront)/account/page.tsx`:**
- Accept `searchParams` prop: `searchParams: Promise<{ page?: string }>`.
- Read page: `const { page: pageParam } = await searchParams;` then `const page = parseInt(pageParam ?? "1");`.
- Calculate pagination bounds: `const pageSize = 20; const from = (page - 1) * pageSize; const to = from + pageSize - 1;`.
- Update orders query to add pagination:
  ```typescript
  const { data: orders, count } = await supabase
    .from("orders")
    .select("*", { count: "exact" })
    .eq("user_id", user.id)
    .order("created_at", { ascending: false })
    .range(from, to);
  ```
- Calculate `const totalOrders = count ?? 0;` and `const totalPages = Math.ceil(totalOrders / pageSize);`.
- Update the order count display in CardDescription to show `totalOrders` instead of `typedOrders.length`.
- Add `<PaginationControls currentPage={page} totalPages={totalPages} baseUrl="/account" />` below the orders table (inside the Card, after CardContent).
- Import `PaginationControls` from `@/components/storefront/pagination-controls`.
- NOTE: PaginationControls is created in plan 03. If plans execute in parallel (wave 1), the component will exist by the time the page renders. If this plan executes first, the import will cause a build error that gets resolved when plan 03 completes. Wave 1 parallel execution is acceptable here since the final tsc check runs after all plans.
  </action>
  <verify>
    <automated>grep -q "range" app/\(storefront\)/account/page.tsx && grep -q "count.*exact" app/\(storefront\)/account/page.tsx && echo "PASS: Account pagination added" || echo "FAIL"</automated>
  </verify>
  <done>Account page orders query uses .range() with { count: 'exact' }. Page renders pagination controls. Order count shows total from database.</done>
</task>

<task type="auto">
  <name>Task 3: Verify TypeScript compilation across all pagination changes</name>
  <files>actions/products.ts, actions/blog.ts, actions/reviews.ts, app/(storefront)/shop/page.tsx, app/(storefront)/blog/page.tsx, app/(storefront)/account/page.tsx</files>
  <action>
Run TypeScript compilation to verify all changes from plans 03 and 04 are type-safe:
- `npx tsc --noEmit`
- Fix any type errors that arise from the return type changes.
- Common issues to watch for:
  - Shop page: previously `products` was `Product[]`, now it's destructured from `{ data, total, page, pageSize }`.
  - Blog page: previously `data` was `BlogPost[]` from `{ data, error }`, now from `{ data, total, page, pageSize, error }`.
  - Any component consuming `getProducts()` or `getBlogPosts()` needs to handle the new return shape.
- Check `getFeaturedProducts()` -- this function should NOT be paginated (it's a fixed limit of 8 featured products). Verify it's unchanged.
- Check `getProductBySlug()` -- single product fetch, should be unchanged.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>TypeScript compiles cleanly. All return type changes are handled correctly by consumers.</done>
</task>

</tasks>

<verification>
- [ ] `getProductReviews()` returns `{ data, total, page, pageSize, error }` and uses `.range()`
- [ ] Account page orders query uses `.range()` with `{ count: "exact" }`
- [ ] Account page reads `page` from searchParams and renders `PaginationControls`
- [ ] `getFeaturedProducts()` is unchanged (no pagination)
- [ ] `getProductBySlug()` is unchanged (single product fetch)
- [ ] Storefront page size is 20 items
- [ ] TypeScript compiles without errors
</verification>

<success_criteria>
Product reviews and account order history have server-side pagination via Supabase `.range()`. Account page renders pagination controls. Default page size is 20. TypeScript compiles cleanly across all pagination changes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-database-performance/04-04-SUMMARY.md`
</output>
