---
phase: 07-testing-foundation
plan: 02
type: tdd
wave: 2
depends_on: [01]
files_modified:
  - tests/unit/actions/checkout.test.ts
  - tests/integration/webhook.test.ts
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - checkout action test suite covers price validation, stock checks, coupon logic, and session creation
    - webhook test suite covers idempotent order creation, stock decrement, coupon increment, and refund handling
    - all tests use mock factories from tests/helpers/
  artifacts:
    - tests/unit/actions/checkout.test.ts
    - tests/integration/webhook.test.ts
  key_links:
    - checkout tests mock Supabase and Stripe via helpers
    - webhook tests mock Stripe event construction and admin Supabase client
---

<objective>
Create comprehensive test suites for checkout session creation and Stripe webhook handler — the most critical code paths in the purchase flow.

Purpose: Checkout and webhook are the core value path (money flows through them). Bugs here directly cause revenue loss or data corruption.
Output: Two test files covering all checkout and webhook scenarios with mocked dependencies.
</objective>

<execution_context>
@/Users/richard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richard/.claude/get-shit-done/references/tdd.md
@/Users/richard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@actions/checkout.ts
@app/api/webhooks/stripe/route.ts
@lib/action-result.ts
@lib/stripe.ts
@lib/supabase/server.ts
@lib/supabase/admin.ts
@lib/constants.ts
@lib/rate-limit.ts
@lib/env.ts
@lib/validations.ts
@tests/helpers/mock-supabase.ts
@tests/helpers/mock-stripe.ts
@tests/helpers/mock-next.ts
@tests/helpers/fixtures.ts

<interfaces>
From actions/checkout.ts:
```typescript
export async function createCheckoutSession(
  items: CartItem[],
  couponCode?: string | null
): Promise<ActionResult<{ url: string }>>
```

From app/api/webhooks/stripe/route.ts:
```typescript
export async function POST(request: NextRequest): Promise<NextResponse>
```

From lib/action-result.ts:
```typescript
export type ActionResult<T> = { success: true; data: T } | { success: false; error: string };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Checkout session test suite (TDD)</name>
  <files>tests/unit/actions/checkout.test.ts</files>
  <action>
Write tests for `createCheckoutSession` in TDD style. Mock dependencies:
- `vi.mock('@/lib/supabase/server')` → return mock Supabase client
- `vi.mock('@/lib/stripe')` → return mock Stripe client
- `vi.mock('@/actions/settings')` → return mock shop settings
- `vi.mock('@/lib/logger')` → silent logger

Test cases to cover:

**Validation:**
1. Returns fail when cart is empty (items: [])
2. Returns fail when item has negative price
3. Returns fail when item has zero quantity

**Price validation (server-side):**
4. Replaces client prices with DB prices (client sends $10, DB has $15 → Stripe gets $15)
5. Returns fail("Item is currently unavailable") when product not found in DB
6. Returns fail("Item is currently unavailable") when product is_active=false
7. Returns fail("Item is currently unavailable") when variant not found
8. Returns fail("Item is currently unavailable") when variant is_active=false

**Stock checks:**
9. Returns fail("Item is currently unavailable") when requested quantity > available stock
10. Succeeds when quantity equals available stock (edge case)

**Coupon logic:**
11. Returns fail("Coupon is not valid") when coupon not found
12. Returns fail("Coupon is not valid") when coupon expired
13. Returns fail("Coupon is not valid") when max uses reached
14. Returns fail("Coupon is not valid") when order below minimum
15. Applies percentage discount correctly (10% off $100 = $90 subtotal)
16. Applies fixed discount correctly ($10 off $100 = $90 subtotal)
17. Fixed discount capped at subtotal (can't go negative)

**Session creation:**
18. Returns ok with URL on successful checkout (no coupon)
19. Returns ok with URL on successful checkout with valid coupon
20. Shipping cost added when subtotal below free_shipping_threshold
21. Free shipping when subtotal >= free_shipping_threshold
22. Line items include correct metadata (product_id, variant_id, original_unit_price)
23. Session metadata includes coupon_code, discount_amount, original_subtotal

**Error handling:**
24. Returns fail with message when Stripe throws

Use fixtures from tests/helpers/fixtures.ts for mock data. Use mock factories from tests/helpers/mock-supabase.ts and mock-stripe.ts.

Each test should set up the specific mock returns needed, call createCheckoutSession, and assert on the ActionResult.
  </action>
  <verify>
    <automated>bun vitest run tests/unit/actions/checkout.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>All checkout tests pass. Coverage of checkout.ts exceeds 80%. Tests verify price validation, stock checks, coupon logic, and session creation.</done>
</task>

<task type="auto">
  <name>Task 2: Webhook handler test suite (TDD)</name>
  <files>tests/integration/webhook.test.ts</files>
  <action>
Write tests for the Stripe webhook POST handler. Mock dependencies:
- `vi.mock('@/lib/stripe')` → mock stripe including webhooks.constructEvent
- `vi.mock('@/lib/supabase/admin')` → mock createAdminClient returning mock Supabase
- `vi.mock('@/lib/rate-limit')` → mock webhookLimiter.check() returning { success: true }
- `vi.mock('@/lib/env')` → mock env with STRIPE_WEBHOOK_SECRET
- `vi.mock('@/lib/logger')` → silent logger

Create NextRequest objects for calling the POST handler directly:
```typescript
const request = new NextRequest('http://localhost/api/webhooks/stripe', {
  method: 'POST',
  body: eventBody,
  headers: { 'stripe-signature': 'valid_sig' },
});
```

Test cases:

**Signature validation:**
1. Returns 400 when stripe-signature header missing
2. Returns 400 when constructEvent throws (invalid signature)

**Rate limiting:**
3. Returns 429 when webhookLimiter returns { success: false }

**checkout.session.completed handler:**
4. Creates order with correct totals from metadata (discount_amount, original_subtotal)
5. Creates order items with original unit prices from product metadata
6. Calls decrement_stock RPC for each line item
7. Calls increment_coupon_uses RPC when coupon_code present
8. Does NOT call increment_coupon_uses when no coupon
9. Idempotency: skips order creation when order with same session ID exists
10. Associates order with user via email lookup in customer_profiles
11. Sets userId to null when no matching customer_profiles entry
12. Separates shipping line item from product line items correctly
13. Returns 200 with { received: true } on success

**charge.refunded handler:**
14. Updates order status to 'refunded' using payment_intent_id
15. Returns 200 on successful refund status update

**Error handling:**
16. Returns 500 when order insert fails
17. Logs error but continues when stock decrement fails (partial fulfillment)
18. Returns 200 for unhandled event types

Use createMockStripeEvent from tests/helpers/mock-stripe.ts to build test events.
  </action>
  <verify>
    <automated>bun vitest run tests/integration/webhook.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>All webhook tests pass. Coverage of webhook route.ts exceeds 80%. Tests verify idempotent order creation, stock decrement, coupon increment, refund handling, and error scenarios.</done>
</task>

</tasks>

<verification>
- `bun vitest run tests/unit/actions/checkout.test.ts` — all pass
- `bun vitest run tests/integration/webhook.test.ts` — all pass
- Combined: `bun vitest run tests/unit/actions/checkout.test.ts tests/integration/webhook.test.ts` — all pass
</verification>

<success_criteria>
- Checkout test suite covers: empty cart, price validation, stock checks, coupon validation (5 failure modes), session creation with/without coupon, shipping logic
- Webhook test suite covers: signature validation, rate limiting, order creation, idempotency, stock decrement, coupon uses, refund handling, error scenarios
- All tests use mock factories from tests/helpers/
</success_criteria>

<output>
After completion, create `.planning/phases/07-testing-foundation/07-02-SUMMARY.md`
</output>
