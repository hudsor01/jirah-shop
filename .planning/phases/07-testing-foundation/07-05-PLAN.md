---
phase: 07-testing-foundation
plan: 05
type: execute
wave: 2
depends_on: [01]
files_modified:
  - tests/unit/lib/format.test.ts
  - tests/unit/lib/action-result.test.ts
  - tests/unit/actions/blog.test.ts
  - tests/unit/actions/coupons.test.ts
  - tests/unit/actions/reviews.test.ts
  - tests/unit/actions/settings.test.ts
  - tests/unit/actions/orders.test.ts
  - tests/unit/actions/contact.test.ts
  - tests/unit/components/__snapshots__/
autonomous: true
requirements: [TEST-08, TEST-09, TEST-11]

must_haves:
  truths:
    - formatPrice, formatDate, formatDateLong utility tests exist and pass
    - ActionResult ok/fail helper tests exist
    - Tests exist for blog, coupon, order, review, settings, and contact server actions
    - Snapshot tests exist for at least 3 critical UI components
  artifacts:
    - tests/unit/lib/format.test.ts
    - tests/unit/lib/action-result.test.ts
    - tests/unit/actions/blog.test.ts
    - tests/unit/actions/coupons.test.ts
    - tests/unit/actions/reviews.test.ts
    - tests/unit/actions/settings.test.ts
    - tests/unit/actions/orders.test.ts
    - tests/unit/actions/contact.test.ts
  key_links:
    - format tests verify locale-specific output
    - action tests use mock factories and ActionResult assertions
---

<objective>
Create utility tests (formatters, ActionResult helpers), remaining server action test suites, and snapshot tests for critical UI components.

Purpose: Quick-win coverage for pure functions, breadth coverage across remaining actions, and regression protection via snapshots.
Output: Formatter tests, ActionResult tests, 6 server action test files, and UI component snapshot tests.
</objective>

<execution_context>
@/Users/richard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@lib/format.ts
@lib/action-result.ts
@actions/blog.ts
@actions/coupons.ts
@actions/reviews.ts
@actions/settings.ts
@actions/orders.ts
@actions/contact.ts
@tests/helpers/mock-supabase.ts
@tests/helpers/mock-next.ts
@tests/helpers/fixtures.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Utility tests and ActionResult tests</name>
  <files>tests/unit/lib/format.test.ts, tests/unit/lib/action-result.test.ts</files>
  <action>
**format.test.ts — Pure function tests (no mocks):**
1. formatPrice(12.99) → "$12.99"
2. formatPrice(0) → "$0.00"
3. formatPrice(1000) → "$1,000.00"
4. formatPrice(0.5) → "$0.50"
5. formatDate("2025-01-15T00:00:00Z") → "Jan 15, 2025"
6. formatDate("2025-12-31T23:59:59Z") → "Dec 31, 2025"
7. formatDateLong("2025-01-15T00:00:00Z") → "January 15, 2025"
8. formatDateLong("2025-06-01T00:00:00Z") → "June 1, 2025"

Note: Date assertions should account for timezone differences by using UTC midnight dates. If locale-dependent output varies in CI, use `.toContain()` for partial matches.

**action-result.test.ts — Pure function tests:**
1. ok(data) returns { success: true, data }
2. ok(null) returns { success: true, data: null }
3. fail("error") returns { success: false, error: "error" }
4. Type narrowing: `if (result.success) result.data` is accessible
  </action>
  <verify>
    <automated>bun vitest run tests/unit/lib/format.test.ts tests/unit/lib/action-result.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>Formatter tests pass for price, date, dateLong. ActionResult ok/fail helpers verified.</done>
</task>

<task type="auto">
  <name>Task 2: Remaining server action tests and snapshot tests</name>
  <files>tests/unit/actions/blog.test.ts, tests/unit/actions/coupons.test.ts, tests/unit/actions/reviews.test.ts, tests/unit/actions/settings.test.ts, tests/unit/actions/orders.test.ts, tests/unit/actions/contact.test.ts</files>
  <action>
For each server action file, create a test file with key operation coverage. These don't need TDD-depth — aim for 3-5 tests per file covering the happy path, auth guard, and one error case.

Mock pattern for all:
- `vi.mock('@/lib/auth')` → requireAdmin succeeds by default
- `vi.mock('@/lib/supabase/server')` → mock createClient
- `vi.mock('next/cache')` → mock revalidatePath, updateTag
- `vi.mock('@/lib/logger')` → silent

**blog.test.ts (actions/blog.ts):**
1. createBlogPost succeeds with valid data
2. createBlogPost fails when not admin
3. updateBlogPost succeeds with valid data
4. deleteBlogPost succeeds with valid ID

**coupons.test.ts (actions/coupons.ts):**
1. createCoupon succeeds with valid data
2. createCoupon fails with invalid discount_value
3. updateCoupon succeeds
4. deleteCoupon succeeds

**reviews.test.ts (actions/reviews.ts):**
1. getProductReviews returns paginated reviews
2. createReview succeeds with valid data
3. rejectReview requires admin

**settings.test.ts (actions/settings.ts):**
1. getShopSettings returns settings object
2. updateShopSettings requires admin and succeeds

**orders.test.ts (actions/orders.ts):**
1. getOrders returns paginated orders
2. getOrder returns single order with items
3. updateOrderStatus requires admin

**contact.test.ts (actions/contact.ts):**
1. submitContactForm succeeds with valid email and message
2. submitContactForm fails with invalid email

**Snapshot tests — create tests/unit/components/snapshots.test.tsx:**
Pick 3 stable data-display components. Good candidates: product card, footer, or any component that renders server data. Render with mock props, call `expect(container).toMatchSnapshot()`. If components require providers, use renderWithCart from helpers.

The snapshot test file should be light — just render and snapshot. Don't test behavior (that's what unit tests are for).
  </action>
  <verify>
    <automated>bun vitest run tests/unit/actions/ tests/unit/components/ 2>&1 | tail -10</automated>
  </verify>
  <done>All remaining action test files created and passing. Snapshot tests exist for at least 3 UI components. Tests cover happy paths, auth guards, and basic error scenarios.</done>
</task>

</tasks>

<verification>
- `bun vitest run tests/unit/` — all tests pass
- Snapshot files generated in __snapshots__/ directories
</verification>

<success_criteria>
- formatPrice, formatDate, formatDateLong have passing tests
- ok() and fail() helper tests pass
- 6 additional server action test files with 3-5 tests each
- At least 3 UI component snapshot tests
</success_criteria>

<output>
After completion, create `.planning/phases/07-testing-foundation/07-05-SUMMARY.md`
</output>
