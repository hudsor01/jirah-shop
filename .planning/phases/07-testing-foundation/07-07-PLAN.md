---
phase: 07-testing-foundation
plan: 07
type: execute
wave: 4
depends_on: [02, 03, 04, 05]
files_modified:
  - vitest.config.ts
  - tests/storage.test.ts
autonomous: true
requirements: [TEST-12, TEST-10]

must_haves:
  truths:
    - "bun run test:coverage" runs and reports at least 30% line coverage
    - existing storage.test.ts mock pattern reviewed and improved if needed
    - coverage thresholds enforced (build fails if below 30%)
  artifacts: []
  key_links:
    - vitest.config.ts coverage thresholds set to 30% minimum
---

<objective>
Validate coverage target of 30% minimum is achieved after all test suites are in place. Review and improve the existing storage.test.ts mock pattern.

Purpose: Coverage gate ensures test quality is maintained. Existing test improvement ensures consistency.
Output: Verified 30% coverage threshold, improved storage test mock pattern.
</objective>

<execution_context>
@/Users/richard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@vitest.config.ts
@tests/storage.test.ts
@tests/helpers/mock-supabase.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Improve storage.test.ts mock pattern</name>
  <files>tests/storage.test.ts</files>
  <action>
Read tests/storage.test.ts and review the mock pattern. The existing tests use `vi.mock('@/lib/supabase/client')` with a module-level `supabase` variable.

Improvements to make:
1. If the mock setup is duplicated or complex, extract to use the shared `createMockSupabaseClient` from tests/helpers/mock-supabase.ts
2. If `vi.mock()` is inside describe blocks, move to top level
3. If beforeEach/afterEach cleanup is missing, add `vi.clearAllMocks()` in beforeEach
4. Ensure all 6 existing tests still pass after refactoring

Be conservative — don't break working tests. If the current pattern is already clean and all tests pass, make minimal changes (add import alias to shared helper if beneficial, add clearAllMocks).
  </action>
  <verify>
    <automated>bun vitest run tests/storage.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>storage.test.ts mock pattern reviewed and improved. All 6 existing tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Run coverage and validate 30% threshold</name>
  <files>vitest.config.ts</files>
  <action>
1. Run full test suite with coverage: `bun run test:coverage`

2. Review coverage output:
   - Check overall line coverage percentage
   - Check branch, function, and statement coverage

3. If coverage is BELOW 30%:
   - Identify the largest uncovered files that are in scope (actions/, lib/, providers/, queries/)
   - Add targeted tests for the most impactful uncovered files
   - Focus on files with the most lines that have zero coverage
   - Possible quick wins: query files in queries/ are pure data access, easy to test with Supabase mock

4. If coverage is ABOVE 30%:
   - Verify thresholds in vitest.config.ts are enforced
   - Run `bun run test:coverage` once more to confirm it passes threshold check

5. Ensure vitest.config.ts coverage.thresholds has:
   ```
   thresholds: {
     lines: 30,
     branches: 30,
     functions: 30,
     statements: 30,
   }
   ```

6. Run final `bun run test:run` to confirm all tests pass without coverage overhead.
  </action>
  <verify>
    <automated>bun run test:coverage 2>&1 | tail -20</automated>
  </verify>
  <done>Coverage reporting shows >= 30% on lines, branches, functions, statements. Thresholds enforced in vitest.config.ts. All tests pass.</done>
</task>

</tasks>

<verification>
- `bun run test:coverage` exits successfully (thresholds met)
- `bun run test:run` passes all tests
- Coverage report generated in coverage/ directory
</verification>

<success_criteria>
- Minimum 30% coverage achieved across lines, branches, functions, statements
- Coverage thresholds enforced in config (CI will fail if coverage drops below 30%)
- Existing storage tests improved and passing
- All test suites from Plans 01-05 pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-testing-foundation/07-07-SUMMARY.md`
</output>
