---
phase: 07-testing-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.config.ts
  - vitest.setup.ts
  - package.json
  - tests/helpers/mock-supabase.ts
  - tests/helpers/mock-stripe.ts
  - tests/helpers/mock-next.ts
  - tests/helpers/fixtures.ts
  - tests/helpers/render-with-providers.tsx
autonomous: true
requirements: [TEST-05, TEST-07, TEST-10]

must_haves:
  truths:
    - vitest coverage configured with v8 provider and 30% threshold
    - tests/helpers/ contains reusable mock factories for Supabase, Stripe, and Next.js
    - existing 6 tests in tests/storage.test.ts still pass
    - "bun run test:coverage" script exists and runs
  artifacts:
    - tests/helpers/mock-supabase.ts
    - tests/helpers/mock-stripe.ts
    - tests/helpers/mock-next.ts
    - tests/helpers/fixtures.ts
    - tests/helpers/render-with-providers.tsx
  key_links:
    - vitest.config.ts coverage block references v8 provider
    - package.json has test:coverage script
---

<objective>
Set up test infrastructure: coverage reporting, global mock factories, test helpers, and improved vitest configuration.

Purpose: Foundation that all subsequent test plans depend on. Mock factories enable consistent mocking across all test suites.
Output: Coverage configuration, mock factories for Supabase/Stripe/Next.js, test fixtures, React render helper with providers.
</objective>

<execution_context>
@/Users/richard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@vitest.config.ts
@vitest.setup.ts
@package.json
@tests/storage.test.ts
@lib/supabase/client.ts
@lib/supabase/server.ts
@lib/stripe.ts
@lib/action-result.ts
@providers/cart-provider.tsx
@types/database.ts

<interfaces>
<!-- Key types the executor needs for mock factories -->

From lib/action-result.ts:
```typescript
export type ActionResult<T> = { success: true; data: T } | { success: false; error: string };
export function ok<T>(data: T): ActionResult<T>;
export function fail(error: string): ActionResult<never>;
```

From providers/cart-provider.tsx:
```typescript
type CartContextValue = {
  items: CartItem[];
  couponCode: string | null;
  itemCount: number;
  subtotal: number;
  shippingCost: number;
  freeShippingThreshold: number;
  total: number;
  addItem: (item: CartItem) => void;
  removeItem: (productId: string, variantId: string | null) => void;
  updateQuantity: (productId: string, variantId: string | null, quantity: number) => void;
  updateItemPrices: (updates: { product_id: string; variant_id: string | null; newPrice: number }[]) => void;
  clearCart: () => void;
  setCoupon: (code: string | null) => void;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure coverage reporting and update vitest config</name>
  <files>vitest.config.ts, package.json</files>
  <action>
1. Install @vitest/coverage-v8: `bun add -d @vitest/coverage-v8`

2. Update vitest.config.ts to add coverage configuration:
   - provider: 'v8'
   - reporter: ['text', 'json', 'html']
   - reportsDirectory: './coverage'
   - include: ['actions/**', 'lib/**', 'providers/**', 'queries/**', 'app/api/**', 'hooks/**']
   - exclude: ['**/*.d.ts', 'tests/**', '**/*.config.*', 'node_modules/**']
   - thresholds: { lines: 30, branches: 30, functions: 30, statements: 30 }

3. Also exclude the tests/e2e/ directory from vitest test matching (vitest shouldn't run Playwright tests):
   - Add `exclude: ['tests/e2e/**', 'node_modules/**']` to the test config

4. Add to package.json scripts:
   - "test:coverage": "vitest run --coverage"
   - "test:run": "vitest run" (if not present)

5. Add 'coverage/' to .gitignore if not already there.

6. Run `bun run test:run` to verify existing 6 tests in storage.test.ts still pass.
  </action>
  <verify>
    <automated>bun run test:run 2>&1 | tail -5</automated>
  </verify>
  <done>Coverage config in vitest.config.ts with v8 provider, 30% thresholds. "test:coverage" and "test:run" scripts in package.json. Existing 6 tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create global mock factories and test helpers</name>
  <files>tests/helpers/mock-supabase.ts, tests/helpers/mock-stripe.ts, tests/helpers/mock-next.ts, tests/helpers/fixtures.ts, tests/helpers/render-with-providers.tsx, vitest.setup.ts</files>
  <action>
1. Create tests/helpers/mock-supabase.ts:
   - Export `createMockSupabaseClient()` that returns a mock with chainable query builder:
     - `.from(table)` returns a chainable object with `.select()`, `.insert()`, `.update()`, `.delete()`, `.eq()`, `.in()`, `.neq()`, `.maybeSingle()`, `.single()`, `.range()`, `.order()`, `.ilike()`, `.or()`, `.contains()`
     - Each chain method returns `this` (mockReturnThis) except terminal methods (single, maybeSingle, order) which resolve to `{ data, error }`
     - `.auth.getUser()` returns `{ data: { user }, error }`
     - `.rpc(name, params)` returns `{ data, error }`
   - Export `createMockAdminClient()` — same shape, for webhook tests using createAdminClient
   - Use a function `chainable(result)` that creates the chain mock, callable to configure per-table results

2. Create tests/helpers/mock-stripe.ts:
   - Export `createMockStripeClient()` returning mock Stripe with:
     - `.checkout.sessions.create()` → resolves to `{ id, url }`
     - `.checkout.sessions.retrieve()` → resolves to full session with line_items
     - `.webhooks.constructEvent()` → returns event object
     - `.products.create()`, `.products.update()`, `.products.del()` — admin product sync
     - `.prices.create()`, `.prices.update()` — variant price sync
   - Export `createMockStripeEvent(type, data)` helper for webhook testing

3. Create tests/helpers/mock-next.ts:
   - Export mocked versions of:
     - `next/cache`: `revalidatePath`, `updateTag` as vi.fn()
     - `next/headers`: `cookies()` returning `{ getAll, set }`, `headers()` returning Map
   - These are meant to be imported and applied via `vi.mock()` in individual test files

4. Create tests/helpers/fixtures.ts:
   - Export test data factories:
     - `mockProduct(overrides?)` — returns Product with sensible defaults (id, name, price, stock_quantity, is_active)
     - `mockVariant(overrides?)` — returns ProductVariant
     - `mockCartItem(overrides?)` — returns CartItem (product_id, variant_id, name, price, quantity)
     - `mockOrder(overrides?)` — returns Order
     - `mockCoupon(overrides?)` — returns coupon with code, discount_type, discount_value, is_active
     - `mockStripeSession(overrides?)` — returns Stripe Checkout Session shape
     - `mockStripeLineItem(overrides?)` — returns Stripe LineItem shape
   - Use type imports from @/types/database

5. Create tests/helpers/render-with-providers.tsx:
   - Export `renderWithCart(ui, options?)` that wraps component in CartProvider
   - Optionally accept initialItems to pre-populate cart via localStorage mock
   - Use @testing-library/react render

6. Update vitest.setup.ts:
   - Keep existing `import '@testing-library/jest-dom'`
   - Add mock for `localStorage` (jsdom provides one but explicitly set up for consistency)
   - Add mock for `window.matchMedia` if not already present (some components may use it)
   - Do NOT add global vi.mock() calls here — those belong in individual test files to keep setup minimal

7. Verify tests/storage.test.ts still passes after all changes.
  </action>
  <verify>
    <automated>bun run test:run 2>&1 | tail -5</automated>
  </verify>
  <done>Mock factories exist in tests/helpers/ for Supabase (chainable), Stripe, and Next.js. Fixtures factory with sensible defaults for Product, CartItem, Coupon, Order, Stripe objects. renderWithCart helper for component testing. All existing tests pass.</done>
</task>

</tasks>

<verification>
- `bun run test:run` passes (existing 6 tests)
- `bun run test:coverage` runs without errors (coverage may be low until more tests added)
- tests/helpers/ directory contains 5 helper files
- vitest.config.ts has coverage block with v8 provider and 30% thresholds
</verification>

<success_criteria>
- Coverage reporting works with v8 provider
- Mock factories are importable and type-safe
- Existing storage tests unaffected
- Infrastructure ready for all subsequent test plans
</success_criteria>

<output>
After completion, create `.planning/phases/07-testing-foundation/07-01-SUMMARY.md`
</output>
